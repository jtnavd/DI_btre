from django.shortcuts import render

def contact(request):
    return

    # contact.save()

    # send_mail(
    #     'Property Listing Inquiry',
    #     'There has been Inquiry for ' + listing + ' Sign into the admin panel for more info',
    #     'travesy.brad@gmail.com',
    #     [realtor_email, 'techguyinfo@gmail.com'],
    #     fail_silently=False
    #     )

    #     messages.success(request, 'Your request has been submitted, a realtor, a realtor will get back to you soon')
    #     return redirect('/listings/'+listing_id)